<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>🏮 Ingeniero — El Farol al Día</title>
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body { background: #07070d; color: #EDE8DF; font-family: 'Segoe UI', system-ui, sans-serif; min-height: 100vh; padding: 12px; max-width: 600px; margin: 0 auto; }
  header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 14px; padding-bottom: 12px; border-bottom: 1px solid #1a1a2e; }
  header h1 { font-size: 16px; color: #FF5500; }
  #ultimo-update { font-size: 10px; color: #555; }
  #semaforo { display: flex; align-items: center; gap: 12px; background: #0f0f1a; border: 1px solid #1a1a2e; border-radius: 12px; padding: 14px 16px; margin-bottom: 12px; cursor: pointer; transition: border-color 0.2s; }
  #semaforo:hover { border-color: #FF5500; }
  #semaforo-luz { width: 16px; height: 16px; border-radius: 50%; flex-shrink: 0; }
  .verde    { background: #22c55e; box-shadow: 0 0 12px #22c55e88; animation: pulso 2s infinite; }
  .rojo     { background: #ef4444; box-shadow: 0 0 12px #ef444488; }
  .amarillo { background: #f59e0b; box-shadow: 0 0 12px #f59e0b88; animation: pulso 1s infinite; }
  @keyframes pulso { 0%,100%{opacity:1} 50%{opacity:0.4} }
  #semaforo-titulo { font-size: 14px; font-weight: 600; }
  #semaforo-sub    { font-size: 11px; color: #777; margin-top: 2px; }
  #semaforo-version { font-size: 11px; color: #FF5500; margin-left: auto; white-space: nowrap; }
  .grid4 { display: grid; grid-template-columns: repeat(4, 1fr); gap: 8px; margin-bottom: 12px; }
  .metrica { background: #0f0f1a; border: 1px solid #1a1a2e; border-radius: 10px; padding: 12px 8px; text-align: center; }
  .metrica-label { font-size: 9px; color: #555; text-transform: uppercase; letter-spacing: 0.5px; margin-bottom: 4px; }
  .metrica-valor { font-size: 22px; font-weight: 700; color: #FF5500; line-height: 1; }
  .metrica-sub   { font-size: 9px; color: #444; margin-top: 3px; }
  .mv-ok     { color: #22c55e !important; }
  .mv-warn   { color: #f59e0b !important; }
  .mv-danger { color: #ef4444 !important; }
  .tabs { display: flex; gap: 4px; margin-bottom: 10px; }
  .tab { flex: 1; padding: 7px; text-align: center; font-size: 11px; border-radius: 6px; cursor: pointer; border: 1px solid #1a1a2e; color: #555; background: transparent; transition: all 0.15s; }
  .tab.active { background: #FF5500; color: #fff; border-color: #FF5500; }
  .tab-content { display: none; }
  .tab-content.active { display: block; }
  .sec { background: #0f0f1a; border: 1px solid #1a1a2e; border-radius: 10px; padding: 14px; margin-bottom: 10px; }
  .sec-titulo { font-size: 10px; color: #555; text-transform: uppercase; letter-spacing: 0.8px; margin-bottom: 12px; display: flex; align-items: center; justify-content: space-between; }
  .sec-badge { background: #1a1a2e; color: #FF5500; padding: 2px 8px; border-radius: 10px; font-size: 10px; }
  .check { display: flex; align-items: center; gap: 10px; padding: 7px 0; border-bottom: 1px solid #13131f; font-size: 12px; }
  .check:last-child { border-bottom: none; padding-bottom: 0; }
  .check-dot { width: 8px; height: 8px; border-radius: 50%; flex-shrink: 0; }
  .dot-verde { background: #22c55e; } .dot-rojo { background: #ef4444; } .dot-amarillo { background: #f59e0b; }
  .check-label { flex: 1; color: #bbb; }
  .cv-ok { color: #22c55e; } .cv-warn { color: #f59e0b; } .cv-err { color: #ef4444; } .cv-dim { color: #555; }
  .btn-grid  { display: grid; grid-template-columns: repeat(2, 1fr); gap: 8px; margin-bottom: 8px; }
  .btn-grid3 { display: grid; grid-template-columns: repeat(3, 1fr); gap: 8px; margin-bottom: 8px; }
  .btn { background: #13131f; border: 1px solid #22223a; color: #CCC; padding: 11px 8px; border-radius: 8px; font-size: 12px; cursor: pointer; text-align: center; transition: all 0.15s; display: flex; flex-direction: column; align-items: center; gap: 4px; width: 100%; }
  .btn:hover { background: #1a1a2e; border-color: #FF5500; color: #fff; }
  .btn:active { transform: scale(0.96); }
  .btn-icon { font-size: 18px; }
  .btn.danger:hover { border-color: #ef4444; background: #1a0a0a; }
  .btn.success:hover { border-color: #22c55e; background: #0a1a0a; }
  .btn:disabled { opacity: 0.4; cursor: not-allowed; transform: none; }
  .noticia { display: flex; gap: 10px; align-items: flex-start; padding: 8px 0; border-bottom: 1px solid #13131f; cursor: pointer; }
  .noticia:last-child { border-bottom: none; }
  .noticia:hover .noticia-titulo { color: #FF5500; }
  .noticia-num { font-size: 10px; color: #444; min-width: 16px; margin-top: 2px; }
  .noticia-titulo { font-size: 12px; color: #CCC; line-height: 1.35; margin-bottom: 3px; }
  .noticia-meta { font-size: 10px; color: #555; display: flex; gap: 6px; flex-wrap: wrap; }
  .ntag { background: #1a1a2e; color: #FF5500; padding: 1px 5px; border-radius: 3px; font-size: 9px; }
  .nvistas { color: #22c55e; }
  #logs-box { background: #070710; border: 1px solid #1a1a2e; border-radius: 8px; padding: 10px; font-family: monospace; font-size: 10px; color: #22c55e; max-height: 160px; overflow-y: auto; line-height: 1.6; }
  .log-warn  { color: #f59e0b; } .log-error { color: #ef4444; } .log-info { color: #60a5fa; }
  #toast { position: fixed; bottom: 16px; left: 50%; transform: translateX(-50%); background: #1a1a2e; border: 1px solid #FF5500; color: #EDE8DF; padding: 10px 18px; border-radius: 8px; font-size: 12px; display: none; z-index: 999; max-width: 88vw; text-align: center; box-shadow: 0 4px 20px #00000088; }
  #modal-overlay { display: none; position: fixed; inset: 0; background: #000000cc; z-index: 100; align-items: center; justify-content: center; }
  #modal-overlay.visible { display: flex; }
  #modal { background: #0f0f1a; border: 1px solid #2a2a3e; border-radius: 12px; padding: 20px; max-width: 320px; width: 90%; margin: 16px; }
  #modal h3 { font-size: 14px; margin-bottom: 8px; color: #FF5500; }
  #modal p  { font-size: 12px; color: #999; margin-bottom: 16px; line-height: 1.5; }
  .modal-btns { display: flex; gap: 8px; }
  .modal-btns button { flex: 1; padding: 10px; border-radius: 8px; border: 1px solid #2a2a3e; cursor: pointer; font-size: 12px; background: #1a1a2e; color: #CCC; }
  .modal-btns .confirm { border-color: #ef4444; color: #ef4444; }
  .loader { display: inline-block; width: 10px; height: 10px; border: 2px solid #222; border-top-color: #FF5500; border-radius: 50%; animation: spin 0.7s linear infinite; }
  @keyframes spin { to { transform: rotate(360deg); } }
  .cargando { display: flex; justify-content: center; padding: 16px; }
  .ref-btn { background: none; border: 1px solid #1a1a2e; color: #555; padding: 5px 10px; border-radius: 6px; font-size: 11px; cursor: pointer; }
  .ref-btn:hover { border-color: #FF5500; color: #FF5500; }
</style>
</head>
<body>
<header>
  <h1>🏮 Ingeniero — El Farol al Día</h1>
  <div style="display:flex;gap:6px;align-items:center">
    <span id="ultimo-update">—</span>
    <button class="ref-btn" onclick="cargarTodo()">↻ Actualizar</button>
  </div>
</header>
<div id="semaforo" onclick="cargarTodo()">
  <div id="semaforo-luz" class="amarillo"></div>
  <div style="flex:1">
    <div id="semaforo-titulo">Verificando sistema...</div>
    <div id="semaforo-sub">Conectando con el servidor</div>
  </div>
  <div id="semaforo-version">—</div>
</div>
<div class="grid4">
  <div class="metrica"><div class="metrica-label">Noticias</div><div class="metrica-valor" id="m-noticias">—</div><div class="metrica-sub">publicadas</div></div>
  <div class="metrica"><div class="metrica-label">Vistas</div><div class="metrica-valor" id="m-vistas">—</div><div class="metrica-sub">totales</div></div>
  <div class="metrica"><div class="metrica-label">Sin pub.</div><div class="metrica-valor" id="m-minutos">—</div><div class="metrica-sub">minutos</div></div>
  <div class="metrica"><div class="metrica-label">Keys IA</div><div class="metrica-valor" id="m-keys">—</div><div class="metrica-sub">activas</div></div>
</div>
<div class="tabs">
  <button class="tab active" onclick="cambiarTab('salud')">🩺 Salud</button>
  <button class="tab" onclick="cambiarTab('acciones')">⚙️ Acciones</button>
  <button class="tab" onclick="cambiarTab('noticias')">📰 Noticias</button>
  <button class="tab" onclick="cambiarTab('logs')">📋 Logs</button>
</div>
<div id="tab-salud" class="tab-content active">
  <div class="sec">
    <div class="sec-titulo">Estado del sistema <span class="sec-badge" id="badge-problemas">Verificando</span></div>
    <div id="checks-salud"><div class="cargando"><div class="loader"></div></div></div>
  </div>
</div>
<div id="tab-acciones" class="tab-content">
  <div class="sec">
    <div class="sec-titulo">Publicación</div>
    <div class="btn-grid">
      <button class="btn success" onclick="accion('rss')"><span class="btn-icon">📡</span>Procesar RSS</button>
      <button class="btn success" onclick="accion('generar')"><span class="btn-icon">✍️</span>Generar Noticia</button>
    </div>
    <div class="btn-grid3">
      <button class="btn" onclick="generarCat('Nacionales')"><span class="btn-icon">🏛️</span>Nacional</button>
      <button class="btn" onclick="generarCat('Deportes')"><span class="btn-icon">⚽</span>Deportes</button>
      <button class="btn" onclick="generarCat('Economía')"><span class="btn-icon">💰</span>Economía</button>
      <button class="btn" onclick="generarCat('Tecnología')"><span class="btn-icon">💻</span>Tecnología</button>
      <button class="btn" onclick="generarCat('Internacionales')"><span class="btn-icon">🌎</span>Internac.</button>
      <button class="btn" onclick="generarCat('Espectáculos')"><span class="btn-icon">🎭</span>Espectác.</button>
    </div>
  </div>
  <div class="sec">
    <div class="sec-titulo">IA y control</div>
    <div class="btn-grid">
      <button class="btn" onclick="toggleIA(true)"><span class="btn-icon">🟢</span>Activar IA</button>
      <button class="btn" onclick="toggleIA(false)"><span class="btn-icon">🔴</span>Pausar IA</button>
    </div>
  </div>
</div>
<div id="tab-noticias" class="tab-content">
  <div class="sec">
    <div class="sec-titulo">Últimas publicadas <span class="sec-badge" id="badge-noticias">—</span></div>
    <div id="lista-noticias"><div class="cargando"><div class="loader"></div></div></div>
  </div>
</div>
<div id="tab-logs" class="tab-content">
  <div class="sec">
    <div class="sec-titulo">Actividad de esta sesión</div>
    <div id="logs-box"><span style="color:#333">Sin actividad aún.</span></div>
  </div>
  <div class="sec">
    <div class="sec-titulo">Errores registrados</div>
    <div id="lista-errores"><div class="cargando"><div class="loader"></div></div></div>
  </div>
</div>
<div id="modal-overlay">
  <div id="modal">
    <h3 id="modal-titulo">⚠️ Confirmar</h3>
    <p id="modal-texto">Esta acción no se puede deshacer.</p>
    <div class="modal-btns">
      <button onclick="cerrarModal()">Cancelar</button>
      <button class="confirm" id="modal-confirm">Confirmar</button>
    </div>
  </div>
</div>
<div id="toast"></div>
<script>
const PIN='311';
const AUTH='Basic '+btoa('director:311');
let tabActual='salud';

function toast(msg,ms=3000){const t=document.getElementById('toast');t.textContent=msg;t.style.display='block';setTimeout(()=>t.style.display='none',ms);}
function log(msg,tipo=''){const box=document.getElementById('logs-box');if(box.querySelector('span'))box.innerHTML='';const d=new Date().toLocaleTimeString('es-DO',{hour:'2-digit',minute:'2-digit',second:'2-digit'});const cls=tipo==='warn'?'log-warn':tipo==='error'?'log-error':tipo==='info'?'log-info':'';box.innerHTML+=`<div class="${cls}">[${d}] ${msg}</div>`;box.scrollTop=box.scrollHeight;}
async function api(ep,method='GET',body=null){try{const opts={method,headers:{'Content-Type':'application/json','Authorization':AUTH}};if(body)opts.body=JSON.stringify(body);const r=await fetch(ep,opts);return await r.json();}catch(e){return{error:e.message};}}
function tiempoAtras(fecha){const min=Math.round((Date.now()-new Date(fecha))/60000);if(min<1)return'ahora mismo';if(min<60)return`hace ${min}min`;const h=Math.floor(min/60),m=min%60;return m>0?`hace ${h}h ${m}min`:`hace ${h}h`;}
function cambiarTab(tab){tabActual=tab;document.querySelectorAll('.tab').forEach((t,i)=>{t.classList.toggle('active',['salud','acciones','noticias','logs'][i]===tab);});document.querySelectorAll('.tab-content').forEach(c=>c.classList.remove('active'));document.getElementById(`tab-${tab}`).classList.add('active');if(tab==='noticias')cargarNoticias();if(tab==='logs')cargarLogs();}

async function cargarStatus(){
  const d=await api('/status');
  if(d.error||!d.version){document.getElementById('semaforo-luz').className='rojo';document.getElementById('semaforo-titulo').textContent='🔴 Servidor no responde';document.getElementById('semaforo-sub').textContent=d.error||'Sin respuesta';return null;}
  document.getElementById('semaforo-luz').className='verde';
  document.getElementById('semaforo-titulo').textContent='✅ Sistema operando correctamente';
  document.getElementById('semaforo-sub').textContent=`${d.noticias} noticias · ${d.rss_procesados} RSS procesados`;
  document.getElementById('semaforo-version').textContent=`V${d.version}`;
  document.getElementById('m-noticias').textContent=d.noticias;
  document.getElementById('m-keys').textContent='4';
  const checks=[
    {dot:d.ia_activa?'verde':'rojo',label:'Inteligencia Artificial',val:d.ia_activa?'Activa y generando':'❌ PAUSADA',cls:d.ia_activa?'cv-ok':'cv-err'},
    {dot:'verde',label:'Base de datos PostgreSQL',val:'Online — noticias intactas',cls:'cv-ok'},
    {dot:d.pexels_api?.includes('✅')?'verde':'amarillo',label:'Pexels (imágenes)',val:d.pexels_api||'—',cls:d.pexels_api?.includes('✅')?'cv-ok':'cv-warn'},
    {dot:d.marca_de_agua?.includes('✅')?'verde':'rojo',label:'Watermark',val:d.marca_de_agua?.includes('✅')?'WATERMARK(1).png activa':'No encontrada',cls:d.marca_de_agua?.includes('✅')?'cv-ok':'cv-err'},
    {dot:d.facebook?.includes('✅')?'verde':'amarillo',label:'Facebook',val:d.facebook||'—',cls:d.facebook?.includes('✅')?'cv-ok':'cv-warn'},
    {dot:d.twitter?.includes('✅')?'verde':'amarillo',label:'Twitter/X',val:d.twitter||'—',cls:d.twitter?.includes('✅')?'cv-ok':'cv-warn'},
    {dot:'verde',label:'AdSense',val:'pub-5280872495839888 activo',cls:'cv-ok'},
    {dot:'verde',label:'Publicación automática',val:'Cada 30 minutos + RSS 3x día',cls:'cv-ok'},
  ];
  const nAlertas=checks.filter(c=>c.dot!=='verde').length;
  const badge=document.getElementById('badge-problemas');
  badge.textContent=nAlertas>0?`${nAlertas} alerta${nAlertas>1?'s':''}`:'Todo OK';
  badge.style.color=nAlertas>0?'#f59e0b':'#22c55e';
  document.getElementById('checks-salud').innerHTML=checks.map(c=>`<div class="check"><div class="check-dot dot-${c.dot}"></div><span class="check-label">${c.label}</span><span class="${c.cls}" style="font-size:11px">${c.val}</span></div>`).join('');
  return d;
}

async function cargarEstadisticas(){const d=await api('/api/estadisticas');if(d.totalVistas!==undefined){const v=d.totalVistas;document.getElementById('m-vistas').textContent=v>=1000000?(v/1000000).toFixed(1)+'M':v>=1000?(v/1000).toFixed(1)+'k':v;document.getElementById('m-minutos').textContent='—';}}
async function cargarNoticias(){const d=await api('/api/noticias');if(!d.noticias?.length){document.getElementById('lista-noticias').innerHTML='<p style="color:#444;font-size:12px;padding:8px 0">Sin noticias aún</p>';document.getElementById('badge-noticias').textContent='0';return;}document.getElementById('badge-noticias').textContent=d.noticias.length;document.getElementById('lista-noticias').innerHTML=d.noticias.slice(0,12).map((n,i)=>`<div class="noticia" onclick="window.open('/noticia/${n.slug}','_blank')"><div class="noticia-num">${i+1}</div><div style="flex:1"><div class="noticia-titulo">${n.titulo}</div><div class="noticia-meta"><span class="ntag">${n.seccion}</span><span>${tiempoAtras(n.fecha)}</span><span class="nvistas">👁 ${n.vistas}</span></div></div></div>`).join('');}
async function cargarLogs(){const d=await api('/api/memoria?pin=311');if(!d.registros?.length){document.getElementById('lista-errores').innerHTML='<p style="color:#444;font-size:12px">Sin errores ✅</p>';return;}const errores=d.registros.filter(r=>r.tipo==='error').slice(0,10);document.getElementById('lista-errores').innerHTML=errores.length?errores.map(e=>`<div class="check"><div class="check-dot dot-rojo"></div><span class="check-label" style="font-size:11px">${e.valor.substring(0,60)}</span><span class="cv-err" style="font-size:11px">${e.fallos}x</span></div>`).join(''):'<p style="color:#444;font-size:12px">Sin errores ✅</p>';}
async function accion(tipo){const map={rss:['/api/procesar-rss','POST',{pin:PIN}],generar:['/api/generar-noticia','POST',{pin:PIN,categoria:'Nacionales'}]};const[url,method,body]=map[tipo];toast('⏳ Ejecutando...');log(`Ejecutando: ${tipo}`,'info');const r=await api(url,method,body);if(r.success||r.mensaje){const msg=r.titulo?`✅ "${r.titulo.substring(0,40)}..."`:  `✅ ${r.mensaje||'Listo'}`;toast(msg,4000);log(msg);}else{toast(`❌ ${r.error||'Error'}`,4000);log(`ERROR: ${r.error}`,'error');}setTimeout(cargarTodo,2000);}
async function generarCat(cat){toast(`⏳ Generando ${cat}...`);log(`Generando: ${cat}`,'info');const r=await api('/api/generar-noticia','POST',{pin:PIN,categoria:cat});if(r.success){toast(`✅ "${r.titulo?.substring(0,40)}..."`,5000);log(`✅ ${r.titulo?.substring(0,50)}`);}else{toast(`❌ ${r.error}`,4000);log(`ERROR: ${r.error}`,'error');}setTimeout(cargarTodo,2000);}
async function toggleIA(activar){const r=await api('/api/admin/config','POST',{pin:PIN,enabled:activar});if(r.success){toast(activar?'🟢 IA activada':'🔴 IA pausada');setTimeout(cargarTodo,1000);}else toast('❌ Error');}
function cerrarModal(){document.getElementById('modal-overlay').classList.remove('visible');}
document.getElementById('modal-overlay').addEventListener('click',function(e){if(e.target===this)cerrarModal();});
async function cargarTodo(){document.getElementById('ultimo-update').textContent=new Date().toLocaleTimeString('es-DO',{hour:'2-digit',minute:'2-digit',second:'2-digit'});await cargarStatus();await cargarEstadisticas();if(tabActual==='noticias')await cargarNoticias();if(tabActual==='logs')await cargarLogs();}
cargarTodo();
log('🏮 Monitor iniciado','info');
setInterval(cargarTodo,60000);
</script>
</body>
</html>
